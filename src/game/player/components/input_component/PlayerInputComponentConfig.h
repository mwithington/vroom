#pragma once

enum PlayerInputComponentType {
  DEFAULT,
  DUMMY
};

/*
struct PlayerInputComponentConfig {
  PlayerInputComponentType type;
  PlayerInputComponentConfig(PlayerInputComponentType type) : type(type) {}
};
*/
